@model IEnumerable<NexxtDent.Models.Tax>

@{
    ViewBag.Title = @Resources.Resource.Tax_ViewIndex_Titulo1;
}

    <h2>@Resources.Resource.Tax_ViewIndex_Titulo1</h2>

<p>
    @*@Html.ActionLink(@Resources.Resource.ButtonNew, "Create", new { }, new { @class="btn btn-primary"})*@
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Impuesto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Rate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Impuesto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Rate)
        </td>
        <td>
            @Html.ActionLink(@Resources.Resource.ButtonEdit, "Edit", new { id=item.TaxId }, new { @class="btn btn-warning"}) |
            @Html.ActionLink(@Resources.Resource.ButtonDetail, "Details", new { id=item.TaxId }, new { @class="btn btn-info"})
            @*@Html.ActionLink(@Resources.Resource.ButtonDelete, "Delete", new { id=item.TaxId }, new { @class="btn btn-danger"})*@
        </td>
    </tr>
}

</table>

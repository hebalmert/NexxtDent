@model IEnumerable<NexxtDent.Models.Register>

@{
    ViewBag.Title = @Resources.Resource.Register_ViewIndex_Titulo1;
}

    <h2>@Resources.Resource.Register_ViewIndex_Titulo1</h2>

<p>
    @*@Html.ActionLink(@Resources.Resource.ButtonNew, "Create", new { }, new { @class="btn btn-primary"})*@
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Recepcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PagoTecnicos)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NotaCobro)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Factura)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Egresos)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Recepcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PagoTecnicos)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NotaCobro)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Factura)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Egresos)
        </td>
        <td>
            @Html.ActionLink(@Resources.Resource.ButtonEdit, "Edit", new { id=item.RegisterId }, new { @class="btn btn-warning"}) |
            @Html.ActionLink(@Resources.Resource.ButtonDetail, "Details", new { id=item.RegisterId }, new { @class="btn btn-info"})
            @*@Html.ActionLink(@Resources.Resource.ButtonDelete, "Delete", new { id=item.RegisterId }, new { @class="btn btn-danger"})*@
        </td>
    </tr>
}

</table>

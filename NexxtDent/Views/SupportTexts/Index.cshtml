@model IEnumerable<NexxtDent.Models.SupportText>

@{
    ViewBag.Title = @Resources.Resource.SupportText_ViewIndex_Titulo1;
}

    <h2>@Resources.Resource.SupportText_ViewIndex_Titulo1</h2>

<p>
    @*@Html.ActionLink(@Resources.Resource.ButtonNew, "Create", new { }, new { @class="btn btn-primary"})*@
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TextoCondiciones)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TextoCondiciones)
        </td>
        <td>
            @Html.ActionLink(@Resources.Resource.ViewEdit, "Edit", new { id=item.SupportTextId }, new { @class="btn btn-warning"}) |
            @Html.ActionLink(@Resources.Resource.ViewDetail, "Details", new { id=item.SupportTextId }, new { @class="btn btn-info"})
            @*@Html.ActionLink(@Resources.Resource.ViewDelete, "Delete", new { id=item.SupportTextId }, new { @class="btn btn-danger"})*@
        </td>
    </tr>
}

</table>

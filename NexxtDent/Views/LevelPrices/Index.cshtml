@model IEnumerable<NexxtDent.Models.LevelPrice>

@{
    ViewBag.Title = @Resources.Resource.LevelPrice_ViewIndex_Titulo1;
}

    <h2>@Resources.Resource.LevelPrice_ViewIndex_Titulo1</h2>

<p>
    @Html.ActionLink(@Resources.Resource.ButtonNew, "Create", new { }, new { @class="btn btn-primary"})
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NivelPrecio)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NivelPrecio)
        </td>
        <td>
            @Html.ActionLink(@Resources.Resource.ButtonEdit, "Edit", new { id=item.LevelPriceId }, new { @class="btn btn-warning"}) |
            @Html.ActionLink(@Resources.Resource.ButtonDetail, "Details", new { id=item.LevelPriceId }, new { @class="btn btn-info"}) |
            @Html.ActionLink(@Resources.Resource.ButtonDelete, "Delete", new { id=item.LevelPriceId }, new { @class="btn btn-danger"})
        </td>
    </tr>
}

</table>
